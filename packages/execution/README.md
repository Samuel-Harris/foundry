# execution

Take a Linear ticket to a merge-ready draft pull request.

## What it installs

| Primitive | Name | Purpose |
| --- | --- | --- |
| Skill | `implement-linear-ticket` | Take a Linear ticket to a merge-ready draft GitHub pull request. |

## Depends on

- `pr`
- `review`

## Install

```bash
apm install Samuel-Harris/foundry/packages/execution#v0.1.0 --target cursor,claude,copilot
```

## Targets

Deployed to `cursor`, `claude` and `copilot`.
