# Provenance

This skill previously vendored the AWS Labs `deploy-on-aws` draw.io generator (Apache-2.0). Output is now Mermaid markdown only, so no vendored generator code ships with this skill. Do not emit `.drawio` files or diagrams.net URLs.
